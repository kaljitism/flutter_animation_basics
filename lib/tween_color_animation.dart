import 'package:flutter/material.dart';

class TweenColorAnimation extends StatelessWidget {
  const TweenColorAnimation({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
